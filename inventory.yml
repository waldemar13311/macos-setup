all:
  hosts:
    macos:
      ansible_host: 192.168.64.77 # Замените на свой IP
      ansible_user: user
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
